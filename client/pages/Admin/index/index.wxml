<view class="admins">
    <text>管理员的权限管理</text>
      <navigator  wx:for="{{datas}}" url="{{item.right_route}}" wx:key="{{index}}">
          <button size='default'><i class="iconfont icon-quanxian"></i>{{item.right_name}}</button>
      </navigator>
</view>